server:
  port: 8080

spring:
  application:
    name: keystone-fsm
  datasource:
    url: jdbc:h2:mem:keystone_dev;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE;MODE=PostgreSQL
    driverClassName: org.h2.Driver
    username: sa
    password: ""
  h2:
    console:
      enabled: true
      path: /h2-console
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update
    show-sql: false
    open-in-view: false
  flyway:
    enabled: false

jwt:
  secret: S3cr3tK3yst0n3FSM_ChangeThisInProductionUseAtLeast32Chars!
  expiration-ms: 86400000
